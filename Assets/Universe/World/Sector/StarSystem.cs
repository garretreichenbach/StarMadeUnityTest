using UnityEngine;

namespace Universe.World.Sector {
	/**
	 * Each star system contains a star in the system (or nothing if void system), and orbits of planets and asteroids,
	 * and is made up of a 16x16 grid of sectors (256 total).
	 */
	public class StarSystem : MonoBehaviour { }
}