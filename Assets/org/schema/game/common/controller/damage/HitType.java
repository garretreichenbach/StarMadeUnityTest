package org.schema.game.common.controller.damage;

public enum HitType {
	WEAPON, //weapon hit
	SUPPORT, //hit by support tool
	ENVIROMENTAL, //sector effects
	INTERNAL, //inner explosion
	GENERAL, //anything else
}
