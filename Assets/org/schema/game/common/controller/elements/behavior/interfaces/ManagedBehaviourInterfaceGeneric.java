package org.schema.game.common.controller.elements.behavior.interfaces;

public sealed interface ManagedBehaviourInterfaceGeneric permits ManagedChargingInterface, ManagedActivationInterface, ManagedCooldownInterface {
    //nothing here for now; may not need this unless it's useful for generified systems networking somehow in the future.
}
